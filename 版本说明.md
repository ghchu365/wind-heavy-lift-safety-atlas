# V2 版本说明

这是从 V1 复制出来的后续开发版本。

## 用途

V2 用于接下来继续修改、优化和新增功能。后续改动应优先在本文件夹中进行，避免影响 V1 冻结版本。

## 创建时间

2026-05-30

## 来源

本文件夹内容来自：

```text
E:\claude desktop\mywebsite-Envision
```

复制时与 V1 内容一致。

## 当前包含内容

已复制当前项目的主要内容，包括：

- `src/` 源码
- `public/` 静态资源
- `package.json`
- `package-lock.json`
- `astro.config.mjs`
- `tailwind.config.mjs`
- `tsconfig.json`
- `.env`
- `safety-form.db`
- `.claude/` 项目运行配置

## 未复制内容

以下目录未复制，因为它们体积较大或可重新生成：

- `node_modules/`
- `.git/`
- `.astro/`
- `dist/`
- `.vercel/`
- `.netlify/`
- `*.log`

## 如果需要运行 V2

进入 V2 文件夹后执行：

```bash
npm install
npm run dev
```

## 后续开发说明

从现在开始，如果需要继续改项目，建议以本文件夹作为工作目录：

```text
E:\claude desktop\V2
```

V1 保持不动，作为稳定备份。

---

## V2 修改记录

### 2026-05-30 — 首页动图替换

已修改：
- `src/pages/index.astro` — 首页英雄区右侧 SVG 动态监控图，已根据参考图重新绘制
  - 红色牵引车 + 叶片工装车
  - 超长风机叶片（带支撑架）
  - 山区弯道绿色山体轮廓
  - 限速 40 标志牌
  - 蓝色天空光晕
  - 运输路径发光轨迹
  - HUD 监控面板
  - 保持深色工业 + 风电运输安全监控风格

当前状态：此 SVG 为临时创作版，下一步待用户提供 SVG 文件后直接替换。

### 已验证
- `npm install` 依赖安装成功
- `npm run dev` 开发服务器在 `http://localhost:4330/` 运行正常
- 首页 HTTP 200，新 SVG 元素已渲染

### 未修改
- V1 目录 (`E:\claude desktop\V1`) 未做任何改动
